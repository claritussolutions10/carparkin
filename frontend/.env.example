VITE_API_URL=http://localhost:5000/api
VITE_GOOGLE_MAPS_API_KEY=
# Public test/live Key ID only (safe to expose client-side) — used by Razorpay
# Checkout.js. The Key Secret stays server-side only, in backend/.env.
VITE_RAZORPAY_KEY_ID=
